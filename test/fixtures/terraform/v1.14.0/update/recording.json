{
  "scenario": "update",
  "description": "A new input of terraform_data, which changes it in place.",
  "cliVersion": "v1.14.0",
  "commands": [
    {
      "id": "plan",
      "argv": [
        "terraform",
        "plan",
        "-input=false",
        "-no-color",
        "-refresh=false",
        "-json",
        "-out={plan}",
        "-var-file=dev.tfvars"
      ],
      "cwd": "network",
      "exitCode": 0,
      "stdout": "plan.stdout",
      "stderr": "plan.stderr",
      "stdoutFormat": "text",
      "env": {
        "TF_WORKSPACE": "dev"
      }
    },
    {
      "id": "show",
      "argv": [
        "terraform",
        "show",
        "-json",
        "-no-color",
        "{plan}"
      ],
      "cwd": "network",
      "exitCode": 0,
      "stdout": "show.stdout",
      "stderr": "show.stderr",
      "stdoutFormat": "json",
      "env": {
        "TF_WORKSPACE": "dev"
      }
    }
  ]
}
